{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug",
            "program": "${fileDirname}${/}dbg${/}out",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "Compile"
        }
    ]
}
